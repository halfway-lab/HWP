# HWP current branch (v0.6 RC2 line):
# Core protocol layer (hwp_protocol/) uses only Python 3.9+ standard library.
# The API server layer (api_server.py) requires the following:
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
